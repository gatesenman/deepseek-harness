/**
 * Public entry of the ontology-studio editor package: the pure editor helpers.
 * The browser app boots through `src/main.tsx` via the package-local Vite
 * config (`pnpm dev`); React components stay internal under `src/client/`.
 * @module @deepseek-ai/dsh-experimental-ontology-studio
 */
export * from './helpers.ts'
